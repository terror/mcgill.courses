#!/usr/bin/env bash

OUTPUT=./client/src/lib/types.ts

typeshare -l typescript -o $OUTPUT .

# Hack to get dates to work
echo 'export interface DateTime {
  $date: {
    $numberLong: string;
  };
};' >> $OUTPUT
