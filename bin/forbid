#!/usr/bin/env bash

set -euo pipefail

which rg

! rg \
  --color always \
  --ignore-case \
  --glob !bin/forbid \
  --glob !package-lock.json \
  --glob !pnpm-lock.yaml \
  --glob !seed \
  --glob !tools/*/poetry.lock \
  'dbg!|fixme|todo|xxx'
